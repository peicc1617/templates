<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--命名空间应该是对应接口的包名+接口名 -->
<mapper namespace="cn.edu.xjtu.cad.templates.dao.NodeRoleMapper">
    <!--id应该是接口中的方法，结果类型如没有配置别名则应该使用全名称 -->


    <insert id="addNodeRole">
        INSERT INTO project_node_role VALUES  (#{projectID},#{nodeIndex},#{userID},#{nodeRole})
    </insert>
    
    <insert id="addAllNodeRole" parameterType="java.util.List">
        INSERT INTO project_node_role (projectID,nodeIndex,userID,nodeRole) VALUES
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.projectID},
            #{item.nodeIndex},
            #{item.userID},
            #{item.nodeRole})
        </foreach>
    </insert>

    <delete id="deleteNodeRole">
        DELETE FROM project_node_role where projectID = #{projectID} AND nodeIndex = #{nodeIndex} AND userID = #{userID}
    </delete>

    <select id="getNodeRole">
        SELECT * FROM project_node_role where projectID = #{projectID} AND nodeIndex = #{nodeIndex} AND userID = #{userID}
    </select>

    <update id="updateNodeRole">
        UPDATE project_node_role SET nodeRole = #{nodeRole.nodeRole} WHERE projectID = #{projectID} AND nodeIndex = #{nodeIndex}AND userID =#{userID}
    </update>

</mapper>